{
    "version": 1,
    "disable_existing_loggers": false,
    "formatters": {
        "simple": {
            "format": "%(levelname)s: %(filename)s file, line no. %(lineno)d -> %(message)s"
        },
        "detailed": {
            "format": "%(levelname)s: %(filename)s file, line no. %(lineno)d at %(asctime)s -> %(message)s",
            "datefmt": "%d-%b-%Y %H:%M:%S %z"
        }
    },
    "handlers": {
        "stdout": {
            "class": "logging.StreamHandler",
            "formatter": "simple",
            "stream": "ext://sys.stdout"
        },
        "rotating_file": {
            "class": "logging.handlers.RotatingFileHandler",
            "formatter": "detailed",
            "filename": "logs/app.log",
            "backupCount": 5,
            "maxBytes": 125000,
            "mode": "a",
            "encoding": "utf8"
        }
    },
    "root": {
        "level": "INFO",
        "handlers": [
            "rotating_file",
            "stdout"
        ]
    }
}